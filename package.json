{
  "name": "way-of-tasting-academy",
  "version": "0.1.4",
  "private": true,
  "workspaces": [
    "web",
    "packages/academy-shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web",
    "typecheck": "npm run typecheck --workspace=web && npm run typecheck --workspace=packages/academy-shared",
    "version:check": "node scripts/check-version.mjs"
  }
}
